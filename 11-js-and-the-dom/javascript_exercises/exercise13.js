// Exercise 13: Add a keydown event to detect when the Enter key is pressed
document.addEventListener('keydown', (e) => {
    if (e.key === 'Enter') {
        console.log('Enter key pressed!');
    }
});
